# Lazygit 統合実装

## 概要

このドキュメントは、lazygit を dotfiles リポジトリに統合する実装の詳細を記録します。

## 背景

### 実装の目的

lazygit は、TUI ベースの Git クライアントで、視覚的に Git 操作を行うことができるツールです。
以下の理由により、dotfiles に統合することにしました：

1. **tmux workflow との統合**: popup 機能により、作業を中断せずに Git 操作が可能
2. **視覚的な操作**: ブランチ切り替え、ステージング、履歴確認などが直感的
3. **複雑な Git 操作**: rebase、cherry-pick、stash などの操作が簡単
4. **既存ツールとの補完**: Git エイリアスや fz 関数と役割分担

### 実装方針

yazi の統合パターンに従い、以下の原則で実装：

1. **XDG Base Directory 準拠**: `.config/lazygit/` に配置
2. **既存の Git 設定を尊重**: `.gitconfig` の設定を上書きしない
3. **tmux workflow とシームレスに統合**: popup で呼び出し可能
4. **最小限の設定から始める**: デフォルトを活用し、段階的に拡張

## 実装内容

### 新規作成ファイル

#### 1. `.config/lazygit/config.yml`

**パス**: `/Users/yuya.matsushima/Project/Personal/dotfiles/.config/lazygit/config.yml`

**内容**:
- Nerd Fonts アイコン有効化（`showIcons: true`, `nerdFontsVersion: "3"`）
- シンプルなテーマ設定（緑/青/赤のカラースキーム）
- Git 設定の尊重（`overrideGpg: false`）
- コミットメッセージ幅 72 文字（Git 設定に合わせる）
- Pull モードを rebase に設定（`.gitconfig` の `pull.rebase = true` に対応）
- ランダムヒント無効化（`showRandomTip: false`）

**設計上の決定**:
- macOS デフォルトの `~/Library/Application Support/lazygit` ではなく、`.config/lazygit/` を使用
- XDG Base Directory 仕様に準拠し、他のツール（yazi, nvim など）と統一
- カスタムコマンドは空配列で開始し、必要に応じて追加可能

#### 2. `.zsh/lazygit.zsh`

**パス**: `/Users/yuya.matsushima/Project/Personal/dotfiles/.zsh/lazygit.zsh`

**機能**:

1. **`lg` エイリアス**: lazygit のクイック起動
   ```bash
   alias lg='lazygit'
   ```

2. **`lgg [path]` 関数**: 指定パスのリポジトリを開く
   ```bash
   lgg ~/path/to/repo
   ```
   - Git リポジトリかどうかを確認（`.git` ディレクトリの存在チェック）
   - エラーハンドリング付き

3. **`lgf` 関数**: fzf で Git リポジトリを選択して開く
   ```bash
   lgf
   ```
   - `fd` で `.git` ディレクトリを検索（最大深さ5）
   - fzf でプレビュー表示（`git log --oneline --graph --all -20`）
   - 選択したリポジトリで lazygit を起動

**依存関係**:
- `lgf` 関数: fzf と fd が必要（両方とも既にインストール済み）

#### 3. `docs/plans/add-lazygit.md`

**パス**: `/Users/yuya.matsushima/Project/Personal/dotfiles/docs/plans/add-lazygit.md`

**内容**: このドキュメント

### 変更ファイル

#### 1. `.tmux.conf`

**変更箇所**: 行78付近（`bind p paste-buffer` の後）

**追加内容**:
```tmux
# lazygit in popup
bind g display-popup -d '#{pane_current_path}' -w90% -h90% -E lazygit
```

**説明**:
- `bind g`: prefix（`Ctrl+E`）の後に `g` を押して起動
- `-d '#{pane_current_path}'`: 現在のパスで lazygit を起動
- `-w90% -h90%`: 画面の 90% サイズで表示
- `-E`: lazygit 終了時に popup を自動で閉じる

**設計上の決定**:
- キーバインド `g` を選択した理由: **g**it の頭文字で覚えやすい
- 既存のキーバインドと衝突しない（`g` は未使用）
- popup サイズ 90%: 最大限の視認性を確保しつつ、popup であることが分かる

#### 2. `bin/link.sh`

**変更箇所**: 行37（`.config/yazi` の後）

**追加内容**:
```bash
TARGETS=( \
         ...
         ".config/yazi" \
         ".config/lazygit" \
         ".hammerspoon" \
       )
```

**説明**:
- `.config/lazygit` を TARGETS 配列に追加
- `.config/` 配下のツールをグルーピング
- `make link` または `bin/link.sh` で自動的に symlink が作成される

#### 3. `.zshrc`

**変更箇所**: 行175（yazi ブロックの後）

**追加内容**:
```bash
if (( $+commands[lazygit] )); then
  [ -f "$HOME/.zsh/lazygit.zsh" ] && source "$HOME/.zsh/lazygit.zsh"
fi
```

**説明**:
- lazygit コマンドが存在する場合のみ読み込み
- `.zsh/lazygit.zsh` ファイルが存在する場合のみ source
- yazi と同じパターンで統一性を保つ

## 使用方法

### 1. tmux popup からの起動（メイン）

tmux セッション内で：

```
Ctrl+E  g
```

**動作**:
1. 90% サイズの popup が表示される
2. 現在のディレクトリで lazygit が起動する
3. Git 操作を行う
4. `q` キーで popup が閉じる
5. 元のペインに戻る

**利点**:
- 作業中のペインレイアウトを維持
- 素早く Git 操作を行って戻れる
- 視覚的なフィードバック

### 2. シェルからの直接起動

#### クイック起動

```bash
lg
```

現在のディレクトリで lazygit を起動します。

#### パス指定起動

```bash
lgg ~/Project/Personal/dotfiles
```

指定したパスで lazygit を起動します。

#### リポジトリ選択起動

```bash
lgf
```

fzf を使って Git リポジトリを選択し、lazygit を起動します。

### 3. lazygit 内での基本操作

lazygit の主要なキーバインド：

- `j/k`: 上下移動
- `h/l`: パネル間の移動
- `Space`: ファイルをステージング/アンステージング
- `c`: コミット（vim が起動）
- `P`: Push
- `p`: Pull
- `[/]`: 前/次のタブ
- `?`: ヘルプ
- `q`: 終了

詳細は [lazygit のドキュメント](https://github.com/jesseduffield/lazygit) を参照してください。

## 既存ツールとの統合

### Git エイリアス（.gitconfig）との役割分担

**Git エイリアス（短い操作）**:
- `git st`: ステータス確認
- `git h`: グラフ表示のログ
- `git co`: チェックアウト
- `git com`: コミット

**lazygit（視覚的・複雑な操作）**:
- ブランチの視覚的な切り替え
- 対話的なステージング
- コミット履歴の確認
- rebase、cherry-pick、stash などの複雑な操作

### fz 関数との役割分担

**fz 関数（素早い検索・選択）**:
- `fz branch`: ブランチ切り替え
- `fz log`: コミット履歴検索・hash コピー

**lazygit（包括的な Git 操作）**:
- 複数のファイルをまとめてステージング
- コンフリクト解消
- インタラクティブな rebase
- Stash の管理

### tmux workflow との統合

**通常の作業フロー**:

1. **コーディング**: tmux ペインでコード編集
2. **テスト実行**: 別のペインでテスト
3. **Git 操作**: `Ctrl+E` → `g` で lazygit を popup 表示
4. **変更をコミット**: lazygit 内で操作
5. **作業に戻る**: `q` で popup を閉じる
6. **続きを実装**: 元のペインで作業継続

**利点**:
- ペインレイアウトを崩さない
- コンテキストスイッチが最小限
- 視覚的なフィードバックを得ながら Git 操作

## セットアップ手順

### 1. シンボリックリンク作成

```bash
cd ~/Project/Personal/dotfiles
make link
```

または手動で：

```bash
ln -s ~/Project/Personal/dotfiles/.config/lazygit ~/.config/lazygit
```

### 2. tmux 設定の再読み込み

tmux セッション内で：

```bash
tmux source-file ~/.tmux.conf
```

または `Ctrl+E` → `r` で再読み込み。

### 3. zsh 設定の再読み込み

```bash
source ~/.zshrc
```

新しいシェルセッションを開始しても OK です。

## 動作確認

### 1. lazygit のインストール確認

```bash
which lazygit
# /opt/homebrew/bin/lazygit
```

lazygit が既に `bin/homebrew/cli.sh` 行31 に含まれているため、インストール済みのはずです。

### 2. 設定ファイルの確認

```bash
ls -la ~/.config/lazygit/config.yml
# lrwxr-xr-x ... ~/.config/lazygit/config.yml -> /Users/yuya.matsushima/Project/Personal/dotfiles/.config/lazygit/config.yml
```

symlink が正しく作成されていることを確認します。

### 3. tmux popup の動作確認

tmux セッション内で `Ctrl+E` → `g` を押します。

**確認項目**:
- ✓ popup が 90% サイズで表示される
- ✓ 現在のディレクトリで lazygit が起動する
- ✓ Nerd Fonts アイコンが表示される
- ✓ `q` キーで popup が閉じる
- ✓ 元のペインに戻る

### 4. シェル統合の動作確認

```bash
# lg エイリアス
lg

# lgg 関数
lgg ~/Project/Personal/dotfiles

# lgf 関数
lgf
```

### 5. Git 操作の確認

lazygit 内で以下を確認：

1. ファイルのステージング（`Space` キー）
2. コミット（`c` キー → vim が起動するか）
3. ブランチの切り替え
4. Push/Pull 操作

## トラブルシューティング

### tmux popup が表示されない

**原因**: tmux のバージョンが古い（3.2 未満）

**確認**:
```bash
tmux -V
# tmux 3.3 または 3.4 など
```

**対処**: tmux 3.2 以上にアップグレード
```bash
brew upgrade tmux
```

### アイコンが表示されない

**原因**: Nerd Font がインストールされていない、または使用されていない

**確認**:
```bash
echo "\ue0a0"  # Git ブランチアイコンが表示されるか
```

**対処**:
- Alacritty または Ghostty の設定で Nerd Font を指定
- `font-symbols-only-nerd-font` は既にインストール済み

### lazygit の設定が読み込まれない

**原因**: XDG_CONFIG_HOME が設定されていない

**確認**:
```bash
echo $XDG_CONFIG_HOME
# 空の場合は未設定
```

**対処**:
lazygit は自動的に `~/.config/lazygit/` を探すので、通常は問題ありません。
もし問題がある場合は、`.zshenv` に追加：
```bash
export XDG_CONFIG_HOME="$HOME/.config"
```

### zsh 関数が読み込まれない

**原因**: `.zshrc` の再読み込みが必要

**対処**:
```bash
source ~/.zshrc
```

または新しいシェルセッションを開始。

## 将来の拡張

実装後、必要に応じて以下を追加可能：

### 1. カスタムコマンド

`.config/lazygit/config.yml` にカスタムコマンドを追加：

```yaml
customCommands:
  - key: 'P'
    command: 'gh pr create --assignee @me --label "Claude Code"'
    context: 'global'
    description: 'Create PR with Claude Code label'

  - key: 'C'
    command: 'gh pr view --web'
    context: 'global'
    description: 'View PR in browser'
```

### 2. Delta 統合

より美しい diff 表示のために delta を統合：

```bash
brew install git-delta
```

`.config/lazygit/config.yml` に追加：
```yaml
git:
  paging:
    pager: delta --dark --paging=never
```

### 3. テーマのカスタマイズ

ターミナルのカラースキームに完全一致するテーマを作成：

```yaml
gui:
  theme:
    lightTheme: false
    activeBorderColor:
      - '#50fa7b'  # ターミナルの緑
      - bold
    # その他のカラー設定
```

### 4. 複数の popup サイズ

異なるサイズの popup を追加：

```tmux
# フルスクリーン
bind G display-popup -d '#{pane_current_path}' -w100% -h100% -E lazygit

# 小さいサイズ
bind C-g display-popup -d '#{pane_current_path}' -w70% -h70% -E lazygit
```

### 5. Git commit template

CLAUDE.md のフォーマットに自動対応する commit template を作成：

`.git-templates/commit-message.txt`:
```
# タイトル (50文字以内)

# - 変更内容の詳細説明
# - 箇条書きで記載

# 🤖 Generated with Claude Code
```

`.gitconfig` に追加：
```ini
[commit]
  template = ~/.git-templates/commit-message.txt
```

## 参考リンク

- [lazygit 公式リポジトリ](https://github.com/jesseduffield/lazygit)
- [lazygit ドキュメント](https://github.com/jesseduffield/lazygit/tree/master/docs)
- [lazygit 設定例](https://github.com/jesseduffield/lazygit/blob/master/docs/Config.md)
- [tmux popup ドキュメント](https://man7.org/linux/man-pages/man1/tmux.1.html)

## 変更履歴

- 2024-XX-XX: 初版作成（lazygit の統合）
